# AGENTS.md

> このファイルは、このリポジトリで作業する Codex に向けたプロジェクト概要です。

## 作りたいもの

**忍者テーマパーク**（忍具屋・修行アトラクション・忍者茶屋の3施設）向けの、「シフト作成が楽しくなる」ゲーミフィケーション型シフト管理アプリ。

- 従業員ごとに**忍者アバター**が存在し、シフトの組まれ方（希望通りか／連勤日数／不慣れな施設への応援かどうか）によって表情が `happy` → `neutral` → `tired` → `unhappy` と変化する
- マネージャー（＝忍者頭領）は各従業員の顔を見るだけで「無理なシフトになっていないか」を直感的に把握できる（＝安全弁としての機能）
- 施設別・日別の**売上データ**とシフト（人員配置・人件費）を連動させ、黒字/赤字の状況を可視化する

> ポートフォリオ上の位置づけ：忍者クイズ・忍者タイプ診断・忍者修行ログ・着物花柄認識と世界観を共有する「忍者テーマ」シリーズの1作品。他作品が個人向け（診断・習慣化ツール）である一方、本作は「施設運営・シフト管理」という業務系テーマで差別化する。

## 解決したい課題

- 誰が：忍者テーマパークの施設責任者・シフト管理担当者（忍者頭領）
- どんな場面で：忍具屋・修行アトラクション・忍者茶屋3施設をまたいだ週次/月次のシフトを、Excelや紙ベースで手作業で組んでいる
- 何に困っているか：
  - 連勤や希望と異なるシフトが数字上では見えても、従業員の疲弊度合いが直感的にわからず、無理なシフトを組んでしまいがち
  - 施設間の応援（掛け持ち）が発生した際、誰がどれだけ不慣れな業務を負担しているか把握しづらい
  - シフト（人件費）と売上の連動が別管理になっており、「黒字の日に手厚く配置できているか／赤字の日に人件費をかけすぎていないか」を直感的に判断しづらい
  - シフト作成自体が単調な"作業"になっており、担当者のモチベーションが上がらない

## 使いたい技術

**方針：無料で、権利的（ライセンス）に問題のない技術のみで構成する。**

- フロントエンド：**React**（MITライセンス、無料。Web版でまず動作検証 → 将来的にReact Nativeでアプリ化も検討）
- アバター生成：**DiceBear**（オープンソース／MITライセンスのSVGアバター生成ライブラリ。無料で表情違いのバリエーションを自動生成できる。忍者らしい見た目に寄せるカスタマイズも検討）
- アイコン：**Lucide**（MITライセンス、無料）
- バックエンド／データ：**Supabase**（オープンソース、Postgresベースの無料枠あり）。2026-08-29に本アプリ専用のSupabaseプロジェクトへ移行済み（従業員・シフト・売上・給与設定・ポスト設定・表記(labels)テーブル、Auth、Edge Functionを利用。認証基盤は他アプリのSupabaseプロジェクトとは分離している）
- ホスティング：**GitHub Pages** または **Vercel無料枠**（費用ゼロ）

## 入力

- 従業員マスタ（氏名、役割、所属施設 `main_facility`、応援可能施設 `cross_trained`、希望勤務日数、希望休み曜日、連勤上限、資格情報 など）
- シフトデータ（日付、施設、開始・終了時刻、希望通りか、備考）
- 施設別・日別の売上／人件費データ（`daily_finance`：施設ごとの `revenue` と `labor_cost`）

開発用のダミーデータは `ninja_park_shift_dummy_data.json` を参照。

## 施設一覧

| facility id | 表示名 | 補足 |
|---|---|---|
| goods | 忍具屋（お土産・グッズ販売） | 掛け持ちのハードルは比較的低い |
| amuse | 修行アトラクション（手裏剣投げ・忍者迷路など） | 「手裏剣・忍具取り扱い研修修了」の資格が必要 |
| cafe | 忍者茶屋 | 掛け持ちのハードルは比較的低い |

## 出力

- シフト表画面（施設別・日別の勤務一覧）
- 忍者アバター表示（シフト内容に応じて表情が変化）
- 黒字/赤字の可視化（例：黒字を維持できた日のボーナス演出、赤字が続いた場合のアラート表示など）
- （将来的に）連勤超過・希望乖離・応援偏りに対する管理者向けの警告表示

## アバターの表情判定ロジック（現状の仮ルール）

| mood | 条件 |
|---|---|
| happy | 希望シフト通りに組めており、連勤3日以内 |
| neutral | 希望とほぼ一致しているが、多少の調整あり（施設ヘルプ1回程度） |
| tired | 連勤4日以上、または不慣れな施設への応援が続いている |
| unhappy | 希望と大きく異なる、休み希望日に出勤、または希望外施設への応援が連続している |

## 開発の進め方（MVP方針）

1. シフト表の表示（CRUD）＋アバター表情ロジックの実装から着手
2. 売上データとの連動・可視化を追加
3. 自動シフト生成や高度な最適化は後回しにし、まずは「手動で組んだシフトを判定して表情を変える」体験を優先する

## ロールベース権限（RBAC）と表記（i18n）管理

2026-08-29に追加。

- **ロール**: `manager`（忍者頭領。全施設・全従業員のシフト作成編集、売上閲覧、給与・ポスト設定、用語管理が可能）／`employee`（自分のシフト閲覧のみ、自分の希望勤務日数・希望休み曜日・アバターの編集が可能）。`public.profiles`（`auth.users`拡張）でユーザーとロール・従業員IDを紐付ける。
- 従業員の自己編集は直接UPDATEを許可せず、`update_my_shift_preferences`/`update_my_avatar`という`security definer`なRPC経由に限定している。
- 新規従業員のログインアカウントはEdge Function `create-employee-account`（マネージャーのみ呼び出し可）で作成する。「スタッフ管理」の新規登録フォームから任意で発行できる。
- **表記管理**: 従業員名・施設名・役割名・資格名を「翻訳」ではなく言語ごとの「表記」として`public.labels`テーブルに保持し、マネージャー専用の「用語管理」タブから編集する。未登録言語は日本語にフォールバックする。

## シフト表の刷新・ドラッグ&ドロップ・自動配置・UI全体の英語対応(2026-09-04追加)

- **シフト表**(`ShiftBoard.tsx`): 行=スタッフ(希望勤務日数・希望休み曜日・連勤上限・対応可能施設バッジ)、列=日付のExcel風グリッドに刷新し、1画面に1か月分を表示。スタッフ行の施設バッジをセルへドラッグして新規配置、配置済みセルのドラッグで移動(空きセル)・入れ替え(配置済みセル)ができる(ペイロードは`src/lib/dragDrop.ts`のJSON形式dataTransfer)。
- **ポスト充足パネル**(`PostCoveragePanel.tsx`): 給与・ポスト設定の曜日×施設必要人数と実配置人数を比較し、不足・過多・不在を右側に一覧表示。クリックで該当日の列へスクロール。
- **自動配置**: `src/lib/autoAssign.ts`の`autoAssignShifts`が、資格・対応可能施設・連勤上限・個別の希望休みを必須条件として除外し、所属施設優先/希望休み曜日回避/均等配分をスコアで考慮しながら不足分のみを埋める簡易貪欲法。既存シフトは変更しない。「スタッフの自動配置」ボタンから実行。
- **UI全体の多言語対応**: 従来はヘッダーのJA/ENトグルが「表記」(labelsテーブル)のみを切り替えていたが、`src/lib/i18n.ts`に静的UI文言辞書(`t()`関数、`useLabelContext().t`経由で利用)を新設し、見出し・ボタン・警告メッセージ・表情判定理由(`mood.ts`はメッセージではなくキー+パラメータの`MoodReason`を返し、表示側で`translateReason()`により翻訳)まで含めて画面全体を英語対応させた。ログイン画面・読み込み中画面などLabelProvider外(未認証時)は`useLocale()`を直接使用する。

## カレンダー期間・従業員のチーム閲覧・希望休みカレンダー(2026-08-30追加)

- 対象期間を2026-08-01〜2026-12-31に拡張(`src/data/calendarRange.ts`の固定値。`finance_revenue`にも同期間分の合成売上データを投入済み、`supabase/seed_data_finance_extension.sql`参照)。日付選択は`DatePicker`+`MonthCalendar`(月めくりカレンダーのポップオーバー)に統一し、日数が増えてもUIが破綻しないようにした。
- 従業員も全員分のシフト(`shifts`)を閲覧できるようRLSを変更(書き込みは引き続きマネージャーのみ)。「マイシフト」タブで、選択日の全施設配置(誰と一緒に働くか)と、自分のシフトがある日は「交代候補」(その施設に対応可能で、その日は空いている忍者)を表示する。全員の表の中で自分の列はゴールドの縦線+背景で列全体を強調(2026-09-24。「誰と一緒か」が見たいので自分専用の表示にはしない方針)。
- **ハマりどころ(2026-09-25)**: 「今日の日付」は必ず`src/lib/format.ts`の`todayLocalIso()`を使う。`new Date().toISOString().slice(0,10)`はUTCなので日本時間0:00〜8:59に前日になる(ヘッダー・マイシフト初期選択日・ポスト充足パネルの今日/今週で発生していた)。
- 従業員は曜日パターンの希望休みに加えて、`desired_off_dates`(特定日付の配列)をカレンダーUIで指定できる。表情判定ロジック(`src/lib/mood.ts`)もこの特定日希望休みを優先度高くチェックする。

## マネージャー向けダッシュボード刷新・従業員向け通知/交代候補改善(2026-09-13追加)

Codexによるレビュー提案をもとに、優先度の高い項目から着手。

- **ポスト充足パネル(マネージャー専用、`PostCoveragePanel.tsx`)**: 警告を「不在→不足→過多」の固定順・日付ごとにグループ化して表示。今日/今週/すべての範囲フィルター、未解決のみ表示トグル、対応済みを一時的に非表示にするチェック機能を追加。
- **自動配置の3ステップ化(マネージャー専用)**: 即時反映だった自動配置を「プレビュー(`AutoAssignPreviewModal.tsx`)→確認→反映」に変更。候補ごとに所属施設/応援・希望休みの曜日への強制配置・連勤日数・資格保有を表示し、件数を確認してから反映する。反映後は変更履歴パネルから直近の反映を個別に取り消せる(`bulkDeleteShifts`)。
- **従業員向け「シフト変更通知」**: `shifts`テーブルに`updated_at`(トリガーで自動更新)を追加し、従業員が最後に確認した時刻(ブラウザのlocalStorageに保持)より新しく更新された自分のシフトを、マイシフト画面のバナー+セルのハイライトで知らせる。「確認しました」で既読にする。
- **交代候補の表示改善(従業員向け)**: 候補を「所属施設を優先→普段の希望休み曜日は後回し→今月の勤務数が少ない人を優先」の順でソートし、所属/応援・普段は休みの曜日・今月の勤務回数をバッジ表示。「文面をコピー」でマネージャーに相談する際の定型メッセージをクリップボードにコピーできる(直接の交代依頼機能は持たせず、引き続きマネージャー経由の相談を前提とする方針を維持)。

## 忍者チーム健全性スコア・シミュレーション(2026-09-13追加)

- **健全性タブ(マネージャー専用、`HealthScoreView.tsx`)**: 選択した月のシフトから表情(happy/neutral/tired/unhappy)を集計し、`src/lib/health.ts`のポイント換算(happy=100/neutral=70/tired=35/unhappy=0の平均)でチーム全体・従業員ごとに0〜100の健全性スコアを算出する。
- **要注意の忍者一覧**: この月に「疲れ気味」または「不満」のシフトが1件以上ある従業員だけを表示(全員良好なら「全員良好です」の空状態)。クリックすると`HealthDetailModal.tsx`で該当シフトの理由(`mood.ts`の`MoodReason`を翻訳)と改善案(`i18n.ts`の`healthSuggestion`)を表示する。
- **シミュレーション**: 各要注意シフトに「この配置を外してシミュレーション」ボタンがあり、そのシフトを除いた`shifts`配列で`computeMoodMap`を再計算し、疲れ気味/不満件数とスコアの変化(反映前→反映後)をその場でプレビューできる(DBへの書き込みは行わない)。プレビュー後に「このシフトを実際に削除する」を押すと初めて反映される。

## 俯瞰モード/個人モード(2026-09-13追加)

- **シフト表の表示モード切替(`ShiftBoard.tsx`)**: ツールバーに「編集モード/俯瞰モード/個人モード」の3択トグルを追加。
  - **編集モード**: 従来通りの詳細グリッド(時刻編集・ドラッグ&ドロップ・自動配置・変更履歴)。自動配置ボタン・履歴パネル・通知トーストはこのモードでのみ表示。
  - **俯瞰モード(新規`ShiftOverviewGrid.tsx`)**: 行=スタッフ・列=日付は同じまま、セルを施設色の小さな色付き四角に置き換えたコンパクトなヒートマップ表示。日付列見出しに不足(赤)/過多(金)/充足(薄翡翠)の状況ドットを表示。セルクリックで既存シフトの編集・空きセルなら新規作成モーダルを開く。
  - **個人モード(新規`PersonalSummaryTable.tsx`)**: 従業員1人1行で「希望勤務日数/週・実勤務日数(月)・最大連勤日数・応援回数」を比較できるサマリー表。最大連勤日数が本人の上限を超えている場合は赤字で強調。
- **共通ロジックの共有化**: `sortEmployeesByFacility`(施設順→氏名順ソート)を`src/data/facilities.ts`へ、ポスト充足の集計(`computeCoverageRows`)と日付ごとの充足サマリー(新規`summarizeCoverageByDate`)を新設の`src/lib/postCoverage.ts`へ切り出し、`ShiftBoard`/`MyShiftsView`/`PostCoveragePanel`/`ShiftOverviewGrid`が重複コードなしに再利用する。

## 9月分ダミーデータ投入(2026-09-13)

- ユーザー作成の9月分シフトデータ(163件)を`shifts`テーブルへ投入済み(8月の68件と合わせて計231件)。フォーマット・検証ルールは9月データ作成時に納品した指示書に準拠。

## チャット機能(業務連絡・個人チャット・グループチャット)(2026-09-19追加)

業務連絡・私的なやり取りを社内LINE等に逃がさず1つのアプリに集約し、セクハラ・パワハラまがいの個別連絡を抑止する目的で追加。

- **参加者の識別子は`employees.id`ではなく`profiles.id`(認証ユーザーID)**: マネージャーアカウントは`employees`行を持たないため(頭領は「従業員」ではない)。`profiles`テーブルのSELECTは全員に開放済み(`0012_chat_profile_identity.sql`)。マネージャーはアバターの代わりに固定アイコン(`ChatPersonAvatar.tsx`、`ShieldHalf`)+表記`header.roleManager`で表示される。
- **業務連絡チャンネル**: `chat_conversations`に`type='broadcast'`の行が常に1件だけ存在する(部分ユニークインデックスで保証)。**2026-09-20から投稿は全員に開放**(電車遅延など従業員発の連絡にも使うため。別チャンネルは新設しない方針)。マネージャーの連絡が埋もれないよう、業務連絡内のマネージャー投稿には「公式」バッジを表示し、手動でピン留め(`chat_messages.pinned_at`、RPC `set_chat_message_pinned`)するとスレッド上部の「ピン留め」欄に固定される。
- **ピン留めの権限(2026-09-20)**: 個人チャット・グループチャットは**参加者なら誰でも**ピン留め/解除できる。**業務連絡チャンネルのみマネージャー限定**(従業員にはピン留め欄は見えるが📌ボタンは出ない)。マネージャーが閲覧のみで参加していない他人の会話ではピン留め不可。権限検査はRPC側(`0015_pin_for_all_participants.sql`)とUI(`ChatThread.tsx`の`canPin`)の両方で行う。
- **タブ順**: チャットタブはアクセスしやすいよう、マネージャーは「シフト表」の次、従業員は「マイシフト」の次に配置(2026-09-20)。
- **マニュアル**: 使い方マニュアルを業務連絡チャンネルにマネージャー名義で投稿済み(2026-09-20、DBへ直接INSERT。送信から24時間を過ぎているため画面からは編集できず、内容を直す場合はDBを更新する)。
- **編集・送信取消(2026-09-24)**: 自分の投稿に限り送信から24時間以内なら編集・取り消し可(LINE同様。RPC `edit_chat_message`/`delete_chat_message`、`0016_chat_message_edit_and_delete.sql`)。取り消すと`chat_messages`の本文・画像パスはnullになり他の参加者には「取り消されました」と表示。**編集前・取り消し前の原文は`chat_message_revisions`(マネージャーのみSELECT可)に残り、マネージャーだけが画面で確認できる**(送信直後の証拠隠し防止)。
- **スマホ表示**: `ChatView.tsx`はmd未満で「会話一覧→スレッド→戻る」の1ペイン切り替え。スレッドは表示中のみ・会話ごとに`key`で作り直す(開いた時点で最新までスクロールするため)。一覧だけ表示中は既読にしない。
- **ハマりどころ(2026-09-24)**: 送信欄のEnter判定は`isComposing`/`keyCode 229`を見ること(日本語の変換確定Enterで誤送信するため)。送信は成功後に入力欄を空にする。
- **個人チャット/グループチャット**: `create_chat_conversation` RPC(security definer)で会話作成+メンバー登録をアトミックに行う(DMは既存の組み合わせがあれば再利用)。マネージャーを含む任意のスタッフ同士で開始できる。
- **マネージャーの全チャット閲覧**: 意図的な設計(セクハラ抑止・証拠保全)。`ChatView.tsx`は「自分の会話」と「全スタッフの会話(閲覧のみ)」を分けて表示し、後者は未読集計に含めず、投稿・スタンプとも不可の完全閲覧専用にする。個人/グループ画面には「マネージャーはすべての会話を閲覧できます」と常時表示し、無断監視にならないようにしている。
- **スタンプ(リアクション)**: 忍者っぽいポジティブ固定6種(🥷👍🎉💪🔥🙏、`chat_message_reactions`、1人1メッセージにつき1個・再タップで取消)のみ。自由入力の絵文字は許可しない(ネガティブ用途の抑止)。
- **画像添付**: 非公開Storageバケット`chat-images`+署名付きURL配信。動画(直接アップロード・YouTube埋め込みとも)は無料枠のストレージ/帯域コストを理由に見送り。
- **Realtime**: このアプリで初めてSupabase Realtimeを導入(`chat_messages`/`chat_message_reactions`を`supabase_realtime`publicationに追加)。クライアントは会話ごとではなく1本のグローバル購読(`useChatStore.ts`)。
- **既知のハマりどころ(2026-09-19)**: Storageポリシーのサブクエリで`storage.foldername(name)`と書くと、`chat_conversations`にも`name`列があるため意図せず会話名(グループ名等)を指してしまい、常にRLS拒否になる。`storage.foldername(objects.name)`のように外側テーブルを明示的に修飾すること(`0013_fix_chat_storage_policy_column_shadowing.sql`)。

## 未確定事項（要確認）

- 対象プラットフォーム（Webアプリ／スマホネイティブアプリのどちらを優先するか）
- 複数店舗（複数テーマパーク）でのマルチテナント対応は未対応（現状は単一パーク・単一Supabaseプロジェクト前提）
- 忍者テーマのビジュアル方向性（コミカル寄りか、シリアスな時代劇寄りか）
- 公開範囲（Vercel(`https://ninja-park-shift.vercel.app/`)にデプロイ済み・パスワード保護等のアクセス制限は現状なし。他アプリと違い開発中はローカル限定という方針ではなく、ユーザーの判断でデプロイ済み。デモアカウントは共通パスワードのため、アクセス制限の要否は要検討）
